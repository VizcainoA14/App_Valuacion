# Variables de entorno de desarrollo (plan FASE_4/4.3).
# NINGUNA variable contiene secretos: la aplicación no habla con servicios
# externos (RNF-08). `.env` no se versiona; este ejemplo sí.
#
# ELECTRON_RENDERER_URL la define electron-vite en desarrollo; no fijarla a mano.
